import { Button } from "../ui/button";

const Navbar = () => {
  return (
    <header>
      <nav>
        <div>Navbar</div>
        <ul>
          <li>Menu</li>
        </ul>
        <div>
          <Button>Login</Button>
        </div>
      </nav>
    </header>
  );
};

export default Navbar;
